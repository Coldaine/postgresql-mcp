# postgres-mcp Environment Configuration
# Copy to .env and customize for your environment

# =============================================================================
# PostgreSQL Connection
# =============================================================================
# Option 1: Connection String
# POSTGRES_URL=postgres://user:password@localhost:5432/database

# Option 2: Individual Settings
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=postgres

# =============================================================================
# Secrets Management (VS Code MCP)
# =============================================================================
# For VS Code MCP integration, set PGPASSWORD in your shell environment.
# 
# Option A: Direct export (less secure, for local dev only):
#   export PGPASSWORD="your-password"
#
# Option B: Use Bitwarden Secrets Manager CLI (recommended):
#   1. Store your PostgreSQL password in Bitwarden Secrets Manager
#   2. Set BWS_ACCESS_TOKEN in your shell:
#      export BWS_ACCESS_TOKEN="your-bitwarden-access-token"
#   3. Fetch and export the password at shell startup:
#      export PGPASSWORD=$(bws secret get <secret-id> | jq -r '.value')
#
# Option C: Use .pgpass file (PostgreSQL standard):
#   Create ~/.pgpass with: hostname:port:database:username:password
#   chmod 600 ~/.pgpass
#   (VS Code MCP config can omit PGPASSWORD; libpq will use .pgpass)

# Connection Pool Settings
POSTGRES_POOL_MIN=2
POSTGRES_POOL_MAX=10

# =============================================================================
# Server Configuration  
# =============================================================================
# Transport type: stdio (default), http, sse
MCP_TRANSPORT=stdio

# HTTP port (for http/sse transports)
PORT=3000

# Log level: debug, info, warn, error
LOG_LEVEL=info

# Tool filter (e.g., "-base,-extensions,+starter")
# MCP_TOOL_FILTER=-base,-extensions,+starter
